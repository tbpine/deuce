using iText.IO.Source;

/// <summary>
/// Looks up a types in this application
/// </summary>
public class DeuceConverter 
{
    public object? LookUpType(object obj, ICacheMaster cacheMaster)
    {
        //Check what type it is and act
        //accordingly
        if (obj is deuce.Interval)
        {
            //Look up interval , 
        }

        return null;
    }
}